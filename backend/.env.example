PORT=4000
CORS_ORIGIN=http://localhost:5173

# MongoDB Atlas
MONGODB_URI=mongodb+srv://<user>:<pass>@bitetrack.xxxxxx.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=amazon_wallet_monitor

# JWT
JWT_SECRET=replace_with_strong_secret
JWT_REFRESH_SECRET=replace_with_strong_refresh_secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Encryption key for credentials (must be 32+ chars)
CREDENTIALS_ENCRYPTION_KEY=replace_with_32+_chars_secret_material

